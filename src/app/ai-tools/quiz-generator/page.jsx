import React from 'react';

export const metadata ={
    title : "Quiz"
}


const page = () => {
    return (
        <div>
            quit generator page
        </div>
    );
};

export default page;