import React from 'react';

export const metadata ={
    title : "Register"
}


const page = () => {
    return (
        <div>
            register page
        </div>
    );
};

export default page;