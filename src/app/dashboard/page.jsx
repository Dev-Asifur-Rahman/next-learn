import React from 'react';

export const metadata ={
    title : "Dashboard"
}

const page = () => {
    return (
        <div>
            dashboard page
        </div>
    );
};

export default page;