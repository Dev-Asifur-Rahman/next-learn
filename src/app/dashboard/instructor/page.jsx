import React from 'react';

export const metadata ={
    title : "Instructor | Dashboard"
}


const page = () => {
    return (
        <div>
            instructor page
        </div>
    );
};

export default page;