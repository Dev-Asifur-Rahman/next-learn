import React from 'react';

export const metadata ={
    title : "Admin | Dashboard"
}

const page = () => {
    return (
        <div>
            admin page
        </div>
    );
};

export default page;