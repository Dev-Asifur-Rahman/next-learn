import React from 'react';

export const metadata ={
    title : "Student | Dashboard"
}


const page = () => {
    return (
        <div>
            student page
        </div>
    );
};

export default page;